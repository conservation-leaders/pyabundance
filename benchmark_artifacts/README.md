# Benchmark Artifacts

Release benchmark and comparison outputs can be preserved here with:

```bash
python scripts/preserve_benchmark_artifacts.py --version 0.8.0
```

Artifacts are local-machine results. They are useful for regression context but are not general performance claims.
