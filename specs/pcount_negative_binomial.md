# Negative-binomial N-mixture / pcount Model

## Clean-room statement

This specification and implementation are clean-room. They use independently written model equations,
public documentation, and black-box output comparison only. Do not copy, translate, inspect, or
paraphrase R/C/C++/TMB/Stan source code from `unmarked` or any GPL package.

## Model

For site `i = 1..M` and visit `j = 1..J`:

```text
N_i ~ NegativeBinomial(mean=lambda_i, size=r)
y_ij | N_i ~ Binomial(N_i, p_ij)
log(lambda_i) = X_i beta
logit(p_ij) = W_ij detection
r = exp(log_r)
```

The latent abundance variance is:

```text
Var(N_i) = lambda_i + lambda_i^2 / r
```

Smaller `r` means greater overdispersion. As `r` becomes large, the latent abundance distribution
approaches Poisson with mean `lambda_i`.

## Mean-size PMF

For `n >= 0`, `mu > 0`, and `r > 0`:

```text
log P(N=n) = lgamma(n + r)
             - lgamma(r)
             - lgamma(n + 1)
             + r * (log(r) - log(r + mu))
             + n * (log(mu) - log(r + mu))
```

## Input shapes

- `y`: sites × visits count matrix, passed as `float64`; `NaN` means missing visit.
- `X`: sites × abundance-parameter design matrix. Include the intercept column explicitly.
- `W`: sites × visits × detection-parameter design tensor.
- `K`: finite upper truncation for latent abundance summation.

## Parameter ordering

```text
theta = [beta..., detection..., log_r]
```

## Likelihood

For one site:

```text
L_i = sum_{N=max(y_i observed)}^K NegativeBinomial(N | mean=lambda_i, size=r)
      product_{j observed} Binomial(y_ij | N, p_ij)
```

Overall log-likelihood:

```text
logL = sum_i log(L_i)
```

The implementation evaluates terms in log space and uses log-sum-exp over latent `N`.

## Missing data

- `NaN` counts are skipped in the detection product.
- Non-missing counts must be non-negative integers.
- Infinite values are invalid.

## K truncation handling

`K` must be at least the maximum observed count. Larger values reduce truncation error at additional
cost. If a site has an observed count above `K`, the implementation raises a clear error.

## Validation expectations

- Shape consistency is checked at the Python and Rust boundaries.
- `mu` and `r` must be positive and finite.
- `log_r` must be finite and is transformed internally with `r = exp(log_r)`.
- Stable inverse-logit is used for detection probabilities.
- Probabilities are clamped away from exact 0 and 1 only for finite logarithms.
- Cached problem objects validate counts and fixed shape data once.

## Benchmark protocol

The NB benchmark uses synthetic data generated by a Gamma-Poisson mixture and optional black-box
comparison to R `unmarked::pcount(..., mixture="NB")`. Coefficient parity is reported only when the
parameter names and ordering are clear; log-likelihood parity is the primary black-box target.
